'use client';
import {useEffect,useRef} from 'react';
export function AtlasFrame(){const ref=useRef<HTMLCanvasElement>(null);useEffect(()=>{const canvas=ref.current!;const parent=canvas.parentElement!;const im=new Image();let alive=true;const draw=()=>{if(!alive||!im.complete||!im.naturalWidth)return;const w=parent.clientWidth,h=parent.clientHeight;if(!w||!h)return;const dpr=Math.min(2,window.devicePixelRatio||1);canvas.width=w*dpr;canvas.height=h*dpr;const c=canvas.getContext('2d')!;c.scale(dpr,dpr);c.imageSmoothingEnabled=false;const cut=10;const xs=[0,cut,w-cut,w],ys=[0,cut,h-cut,h];for(let row=0;row<3;row++)for(let col=0;col<3;col++){if(row===1&&col===1)continue;const dw=xs[col+1]-xs[col],dh=ys[row+1]-ys[row];c.save();c.translate(xs[col]+(col===2?dw:0),ys[row]+(row===2?dh:0));c.scale(col===2?-1:1,row===2?-1:1);c.drawImage(im,col===1?458:447,row===1?312:300,col===1?30:8,row===1?30:8,0,0,dw,dh);c.restore();}};im.onload=draw;im.src='/assets/sheet-6.webp?v=raccoon-20260914';const observer=new ResizeObserver(draw);observer.observe(parent);return()=>{alive=false;observer.disconnect();im.onload=null}},[]);return <canvas ref={ref} className="atlas-frame" aria-hidden="true"/>}
