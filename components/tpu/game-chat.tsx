'use client';
import {useEffect,useRef,useState} from 'react';
import {MessageCircle,X,Send} from 'lucide-react';
import {Frame} from './world-assets';
type Message={id:string;username:string;message:string;created_at:number};
export function GameChat({onTyping}:{onTyping:(value:boolean)=>void}){const [open,setOpen]=useState(false),[messages,setMessages]=useState<Message[]>([]),[draft,setDraft]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState('');const bottom=useRef<HTMLDivElement>(null);useEffect(()=>{if(!open)return;let live=true,inFlight=false;let idle=0,lastId="";async function load(){if(document.hidden||inFlight)return;if(idle++%3!==0&&idle>4)return;inFlight=true;try{const r=await fetch('/api/community');const d=await r.json() as {messages:Message[];error:string};if(!r.ok)throw Error(d.error);if(live){const id=d.messages.at(-1)?.id??'';if(id!==lastId){idle=0;lastId=id;setMessages(d.messages);}setError('')}}catch(e){if(live)setError((e as Error).message)}finally{inFlight=false}}load();const timer=setInterval(load,3000);return()=>{live=false;clearInterval(timer)}},[open]);useEffect(()=>{bottom.current?.scrollIntoView({block:'nearest'})},[messages.at(-1)?.id]);return <section className={`game-chat ${open?'expanded':''}`} onPointerDown={e=>e.stopPropagation()}><Frame/><button className="chat-toggle" aria-expanded={open} onClick={()=>{setOpen(v=>!v);onTyping(false)}}><MessageCircle size={18}/> TOWN CHAT {open?<X size={16}/>:<span>OPEN</span>}</button>{open&&<><div className="chat-history" role="log" aria-live="polite" aria-label="Town chat">{messages.length?messages.map(m=><p key={m.id}><time>{new Date(m.created_at).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'})}</time> <b>{m.username}</b><span>{m.message}</span></p>):<p>No messages yet. Say hello to Trash Town.</p>}<div ref={bottom}/></div>{error&&<p className="error" role="alert">{error}</p>}<form onSubmit={async e=>{e.preventDefault();if(!draft.trim()||busy)return;setBusy(true);try{const r=await fetch('/api/community',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:draft})});const d=await r.json() as {messages:Message[];error:string};if(!r.ok)throw Error(d.error);setMessages(d.messages);setDraft('');setError('')}catch(e){setError((e as Error).message)}finally{setBusy(false)}}}><input aria-label="Message to players" placeholder="Say something…" maxLength={400} value={draft} onFocus={()=>onTyping(true)} onBlur={()=>onTyping(false)} onChange={e=>setDraft(e.target.value)} onKeyDown={e=>{e.stopPropagation();if(e.key==='Escape'){e.currentTarget.blur();onTyping(false)}}}/><button aria-label="Send message" disabled={busy||!draft.trim()}><Send size={18}/></button></form><small>Public town channel · {draft.length}/400</small></>}</section>}
