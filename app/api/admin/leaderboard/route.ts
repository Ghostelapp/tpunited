import {db,json,fail,sameOrigin,HttpError,readJson} from '@/lib/server';import {adminIdentity,audit} from '@/lib/admin';import {rankQuery,rulesSchema,defaultRules,type Season} from '@/lib/leaderboard';import {z} from 'zod';
export async function GET(req:Request){try{await adminIdentity(req);const id=new URL(req.url).searchParams.get('season');if(id){const s=await db().prepare("SELECT id FROM seasons WHERE id=? AND status='frozen'").bind(id).first();if(!s)throw new HttpError('Freeze the season before exporting.',409);const result=await db().prepare('SELECT username,wallet,rank,points,active_days,eligible,share FROM season_final WHERE season_id=? ORDER BY rank LIMIT 10001').bind(id).all();if(result.results.length>10000)throw new HttpError('Export is limited to 10,000 participants. Contact the operator for a paged export.',409);return json({rows:result.results});}const seasons=await db().prepare('SELECT * FROM seasons ORDER BY created_at DESC LIMIT 100').all();const exclusions=await db().prepare('SELECT x.*,r.username FROM season_exclusions x JOIN registrations r ON r.user_id=x.user_id ORDER BY x.created_at DESC LIMIT 200').all();return json({seasons:seasons.results,exclusions:exclusions.results,defaults:defaultRules});}catch(e){return fail(e)}}
export async function POST(req:Request){try{sameOrigin(req);const actor=await adminIdentity(req);const b=z.record(z.unknown()).parse(await readJson(req));const action=z.enum(['create','edit','start','freeze','exclude','restore']).parse(b.action),now=Date.now();let id=z.string().uuid().optional().parse(b.id);if(action==='create'||action==='edit'){const v=z.object({title:z.string().trim().min(3).max(80),days:z.number().int().min(1).max(90),rules:rulesSchema}).parse(b);if(v.rules.minDays>v.days)throw new HttpError('Minimum active days cannot exceed season duration.');if(action==='create'){id=crypto.randomUUID();await db().batch([db().prepare("INSERT INTO seasons(id,title,status,rules,starts_at,ends_at,created_at) VALUES(?,?,'draft',?,0,?,?)").bind(id,v.title,JSON.stringify(v.rules),v.days*86400000,now),audit(actor,'season_create',id)]);}else{if(!id)throw new HttpError('Select a draft.');const result=await db().prepare("UPDATE seasons SET title=?,rules=?,ends_at=? WHERE id=? AND status='draft'").bind(v.title,JSON.stringify(v.rules),v.days*86400000,id).run();if(!result.meta.changes)throw new HttpError('Only draft rules can be edited.',409);await audit(actor,'season_edit',id).run();}return json({ok:true,id});}
 if(!id)throw new HttpError('Select a season.');const s=await db().prepare('SELECT * FROM seasons WHERE id=?').bind(id).first<Season>();if(!s)throw new HttpError('Season not found.',404);
 if(action==='start'){if(s.status!=='draft')throw new HttpError('Season has already started.',409);try{const r=await db().prepare("UPDATE seasons SET status='active',active_slot=1,starts_at=?,ends_at=?+ends_at WHERE id=? AND status='draft'").bind(now,now,id).run();if(!r.meta.changes)throw new HttpError('Season changed.',409);}catch{throw new HttpError('Freeze the previous season before starting another.',409);}}
 else if(action==='freeze'){if(s.status!=='active')throw new HttpError('Only an active or ended season can be frozen.',409);const q=rankQuery(s);const frozen=await db().batch([db().prepare("UPDATE seasons SET status='frozen',active_slot=NULL,frozen_at=?,ends_at=MIN(ends_at,?) WHERE id=? AND status='active'").bind(now,now,id),db().prepare(`INSERT INTO season_final(id,season_id,user_id,username,wallet,rank,points,active_days,eligible,share) SELECT lower(hex(randomblob(16))),?,user_id,username,wallet,rank,points,active_days,eligible,CAST(share AS TEXT) FROM (${q.sql}) WHERE changes()>0 ON CONFLICT(season_id,user_id) DO NOTHING`).bind(id,...q.args)]);if(!frozen[0].meta.changes)throw new HttpError('Season already frozen.',409);}
 else{if(s.status!=='active')throw new HttpError('Frozen results cannot be edited.',409);const username=z.string().trim().min(3).max(20).parse(b.username).toLowerCase();const user=await db().prepare('SELECT user_id FROM registrations WHERE username=? COLLATE NOCASE').bind(username).first<{user_id:string}>();if(!user)throw new HttpError('Player not found.',404);if(action==='exclude'){const reason=z.string().trim().min(5).max(300).parse(b.reason);await db().prepare("INSERT INTO season_exclusions(id,season_id,user_id,reason,created_at) SELECT ?,?,?,?,? WHERE EXISTS(SELECT 1 FROM seasons WHERE id=? AND status='active') ON CONFLICT(season_id,user_id) DO UPDATE SET reason=excluded.reason WHERE EXISTS(SELECT 1 FROM seasons WHERE id=? AND status='active')").bind(crypto.randomUUID(),id,user.user_id,reason,now,id,id).run();}else await db().prepare("DELETE FROM season_exclusions WHERE season_id=? AND user_id=? AND EXISTS(SELECT 1 FROM seasons WHERE id=? AND status='active')").bind(id,user.user_id,id).run();}
 await audit(actor,'season_'+action,id).run();return json({ok:true});}catch(e){return fail(e)}}
